# Task Overview: Add Social Links to Hero Section

The user requested to add GitHub, LinkedIn, and an email contact button to the hero section of the index page.

## Changes Made:
- **Modified** `app/pages/index.vue`:
    - Updated the primary call-to-action buttons.
    - Added a group of social links using `UButton` with icons.
    - Included `aria-label` for accessibility.

## Technical Decisions:
- **Consistent Styling**: Used Nuxt UI's `UButton` to match the existing design language.
- **Icon Set**: Used `simple-icons` for GitHub and LinkedIn, and `heroicons` for the envelope icon.
- **Layout**: Grouped social icons together with a slight margin to separate them from the main page navigation buttons.

## Verification:
- Visual check of the code for correct URLs and property usage.
- Ensured the `to` prop is used for external links and mailto.
